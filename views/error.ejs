<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: #0f0f1e;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        .star {
            position: absolute;
            background-color: #ffffff;
            width: 2px;
            height: 2px;
            border-radius: 50%;
            opacity: 0;
            animation: twinkle 2s infinite;
        }
        @keyframes twinkle {
            0%, 100% { opacity: 0; }
            50% { opacity: 1; }
        }
        .container {
            text-align: center;
            color: #ffffff;
        }
        .error-code {
            font-size: 150px;
            font-weight: bold;
            margin: 0;
            line-height: 1;
            position: relative;
            animation: scale 2s infinite alternate ease-in-out;
        }
        @keyframes scale {
    0% { transform: scale(1); }
    100% { transform: scale(1.5); }
}
        .error-code::after {
            content: "404";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            color: #00ffff;
            animation: glitch 1s infinite;
        }
        @keyframes glitch {
            0% { clip-path: inset(50% 0 30% 0); }
            20% { clip-path: inset(33% 0 66% 0); }
            40% { clip-path: inset(10% 0 61% 0); }
            60% { clip-path: inset(83% 0 5% 0); }
            80% { clip-path: inset(92% 0 43% 0); }
            100% { clip-path: inset(58% 0 3% 0); }
        }
        .message {
            font-size: 24px;
            margin: 20px 0;
            text-shadow: 0 0 10px #00ffff;
        }
        .home-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #00ffff;
            color: #0f0f1e;
            text-decoration: none;
            border-radius: 5px;
            transition: all 0.3s;
            font-weight: bold;
        }
        .home-link:hover {
            background-color: #ffffff;
            box-shadow: 0 0 20px #00ffff;
        }
        .astronaut {
            width: 100px;
            height: 100px;
            background: url('https://cdn-icons-png.flaticon.com/512/3468/3468299.png') no-repeat center center;
            background-size: contain;
            margin: 20px auto;
            animation: float 3s ease-in-out infinite;
            filter: invert(1) hue-rotate(180deg);
        }
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body>
    <div class="stars"></div>
    <div class="container">
        <h1 class="error-code">404</h1>
        <div class="astronaut"></div>
        <p class="message">Oops! Looks like you're lost in the cosmic void.</p>
        <a href="/" class="home-link">Return to Earth</a>
    </div>

    <script>
        function createStars() {
            const starsContainer = document.querySelector('.stars');
            const starCount = 200;

            for (let i = 0; i < starCount; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                star.style.left = `${Math.random() * 100}%`;
                star.style.top = `${Math.random() * 100}%`;
                star.style.animationDelay = `${Math.random() * 5}s`;
                starsContainer.appendChild(star);
            }
        }

        createStars();
    </script>
</body>
</html>